include: package:flutter_lints/flutter.yaml

linter:
  rules:
    avoid_print: false  # demo app uses print for the headless demo path
    prefer_const_constructors: true
    require_trailing_commas: true

analyzer:
  errors:
    invalid_annotation_target: ignore
